(() => { 'use strict'; const items=[...document.querySelectorAll('.reveal,.card,.step,.panel')]; const obs=new IntersectionObserver((entries)=>{entries.forEach((e)=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target);}})},{threshold:.12}); items.forEach((el)=>obs.observe(el)); if(!matchMedia('(prefers-reduced-motion: reduce)').matches){document.querySelectorAll('.card').forEach((card)=>{card.addEventListener('pointermove',(event)=>{const r=card.getBoundingClientRect();card.style.setProperty('--x',`${event.clientX-r.left}px`);card.style.setProperty('--y',`${event.clientY-r.top}px`);});});}})();
